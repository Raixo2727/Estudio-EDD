# Selection sort

Debe crear el el algoritmo selection sort:

```pseudocode
SelectionSort(A):
    Definir secuencia B, inicialmente vacía.
    Buscar el menor dato x en A
    Sacar x de A e insertarlo al final de B
    Si quedan valores en A, repetir el paso 2
    return B
```